--- Makefile.in.orig	Sun Feb 25 17:30:41 2007
+++ Makefile.in	Sun Feb 25 17:32:28 2007
@@ -86,9 +86,7 @@
 DATA = $(pkgconfig_DATA)
 ETAGS = etags
 CTAGS = ctags
-DIST_SUBDIRS = po libbeagle wrappers Util glue BeagleClient beagled \
-	Filters images search ImLogViewer mozilla-extension tools \
-	epiphany-extension bludgeon ContactViewer
+DIST_SUBDIRS = libbeagle wrappers
 DISTFILES = $(DIST_COMMON) $(DIST_SOURCES) $(TEXINFOS) $(EXTRA_DIST)
 distdir = $(PACKAGE)-$(VERSION)
 top_distdir = $(distdir)
@@ -380,9 +378,7 @@
 AUTOMAKE_OPTIONS = 1.8
 pkgconfigdir = $(prefix)/libdata/pkgconfig
 pkgconfig_DATA = beagle-0.0.pc beagle-daemon.pc $(am__append_1)
-SUBDIRS = $(am__append_2) $(am__append_3) wrappers Util glue \
-	BeagleClient beagled Filters $(am__append_4) mozilla-extension \
-	tools $(am__append_5) $(am__append_6) $(am__append_7)
+SUBDIRS = $(am__append_3) wrappers
 
 # When the chooser work has been fixed up to use libbeagle, we can
 # reenable this directory.
